import { FC } from 'react'
import { NextPage } from "next";
import * as Styled from './styles'

const {{pascalCase name}}: NextPage = () => {
  return <Styled.Root>{{pascalCase name}}</Styled.Root>
}

export default {{pascalCase name}}
